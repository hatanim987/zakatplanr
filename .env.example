# Neon Postgres connection string (get from Vercel dashboard or Neon console)
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Simple password protection (leave empty to disable auth in dev)
APP_PASSWORD=
